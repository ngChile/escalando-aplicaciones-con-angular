import { FilterActivesPipe } from './filter-actives.pipe';

describe('FilterActivesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterActivesPipe();
    expect(pipe).toBeTruthy();
  });
});
